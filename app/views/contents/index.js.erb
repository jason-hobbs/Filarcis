//showLoader = setTimeout("$('.spinner').show()", 300);
$(".spinner").show();
$(".project-contents").empty();
$(".breadcrumbs").empty();
$("#treetop").empty();
$("<%= escape_javascript(render 'crumb') %>").appendTo(".breadcrumbs");
$("<%= escape_javascript(render 'treetop') %>").appendTo("#treetop");
$("<%= escape_javascript(render 'entries') %>").appendTo(".project-contents");
//clearTimeout(showLoader);
$('.spinner').hide();
//$(".spinner").delay(3000).hide(0);
$('.folder-nav').removeClass('folder-nav-active');
$('#<%= @project.id %>').addClass('folder-nav-active');
window.history.pushState("object or string", "Title", "/projects/<%= @project.id %>/contents");
doDraggable();
dropDown();
clickCheckBoxes();
$('.best_in_place').on("ajax:success", function () {
  location.reload();
});
