<%= form_for [@project, @content] do |f| %>
  <% if @content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content.errors.count, "error") %> prohibited this file from being saved:</h2>

      <ul>
      <% @content.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>




    <!-- <%= f.label "Upload", class: "bold" %><br> -->
    <%= f.file_field :location, multiple: true, name: "content[location]" %>
  

  <!-- <%= f.submit class: 'button tiny radius' %> -->



<% end %>
